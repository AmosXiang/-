# UI 重设计 — 会话交接（2026-07-16 · 全批次收官）

> 新对话开场把本文件读一遍即可接续。协调人 = CC（Claude Code）；重活外包给 Codex/Antigravity。

## 现在在哪

- **主线分支**：`feature/camera-derive`，HEAD = `a38dea2`（已 push origin），工作区**干净**。Video Lab M1+M2 已合入接线，完结 worktree/分支已清。
- **UI 重设计全批次完成**：P0 止血 → P1 路由 → P1b 布局 → P2a 故事版本/定稿 → P2b 交付导出 → **P3 风格契约+参数快照+检查器五区** → **P3.5 场景参考轻量版** → **P4 角色资产+README** → **P4b scenes/+Unicode**。
- **用户四大痛点已闭环**：①上传优先首页 ②分析/创作结构分离+路由化 ③画风统一（项目级风格契约锁 preset/overlay/宽高/LoRA，分镜只开结构参数，批量生成硬闸门，每次生成留参数快照）④交付物（PPTX 分镜手册 + manifest + finals/ + characters/ + scenes/ + README）。
- 远期未排：Video Lab（集成计划裁决 #12，分镜链路已稳，随时可从 `storyboard-manifest.json` 接上）。

## 已合入工作包（全部 review PASS）

WP-B(shot-review) · WP-C(export-deck) · WP-E(story-version) · WP-F(delivery-ui) · WP-G(style-contract) · WP-H(p3-wiring) · WP-I(scene-reference) · WP-J(export scenes/+Unicode) · WP-P4(角色资产)，加 CC 自做的 P0/P1/P1b 与全部接线/集成/回归。

## 权威文档（都在 `docs/ui-redesign/`）

- 方案 v2.1：`workflow-redesign-2026-07-14.md`
- 集成计划 + 12 项设计裁决 + 批次表 + 协作纪律：`tasks/integration-plan-2026-07-14.md`
- 各工作包契约：`tasks/codex-*.md`、`tasks/antigravity-*.md`
- 验收证据：`tasks/evidence/*.md`（每个工作包一份）
- CC 持久记忆 `workflow-redesign-2026-07`，状态同步。

## 铁律（踩过坑固化的，务必守）

1. **主工作区归 CC；外部 agent 强制 `git worktree add`**，禁止在主工作区切分支——违反过一次，CC 的提交落错分支，做过手术。**热区规则（2026-07-15 用户拍板）**：前端热区（App.tsx 等）可向 Codex 放权，但仅限任务书列明的区域，CC 逐行严审；分工 = CC review/合并/回归，Codex+Antigravity coding/验证/提交。
2. **视觉/交互验收以复核方(CC)为准**，agent 自报 PASS 不作数（Antigravity 自报 COM 渲染通过却没拦住画布越界，就是这条的由来；后续多次靠这条拦下 overlay 双注入、证据循环论证等问题）。
3. **合并只经集成分支**，CC 统一执行 + 回归(lint + 全量 node:test + 真机冒烟)，再 ff 回主线。
4. 外包套路（已验证 8+ 次）：**Codex/Antigravity 写后端模块 + 独立前端组件 + 任务书授权的热区触点；CC 出精确任务书 + 逐行 review + 接线兜底**。后端模块照抄 `server/modules/camera-derive|style-contract/` 结构；server.ts 每包只许 1 行 import + 1 行 register（引用其导出纯函数如 `resolveEffectiveStyleContract` 可以，改不行）。
5. 组件签名**别写显式 `JSX.Element`**（仓库没装 `@types/react`，会炸类型检查）。
6. 分镜/项目存 SQLite `store` 表 `key='generated_scripts'` 的 JSON，**没有表、禁止建表**；新字段走 Shot/GeneratedScriptRecord 的可选 JSON 字段(旧数据零迁移)。契约/场景等模块字段同此。

## 环境备忘

- dev 启动：Browser 工具 `preview_start {name:"dev"}`（走 `.claude/launch.json`）。vite 端口常被占会自动换，express 后端 = vite 端口 + 1。**绝不用 Bash 跑 dev server**。改后端后需 `preview_stop` 再 `preview_start` 才重载。
- lint：`npm run lint`（= `tsc --noEmit`）。测试：`npx tsx --test <模块>/*.test.ts`（node:test，`:memory:` SQLite + mkdtemp 隔离）。核心 server.ts 无独立测试基建 → 走 db 副本 curl 证据（禁碰正式 db.sqlite）。
- 本机装了 LibreOffice：PPTX 可 `soffice --headless --convert-to pdf` 渲染做视觉复核。中文 zip 解压核验：其 `createExportZip` 产包 + PowerShell `Expand-Archive`（.NET 解码=资源管理器同源）。

## 下一步候选（无强制排期，按需开工）

- **Video Lab**（裁决 #12）：**方案已定稿** → `docs/ui-redesign/video-lab-plan-2026-07-15.md`（v1.0，用户拍板 + CC review 合并）。WP-Animatic（交付域播放器）可立即立项；Video Lab 从 M1 起立项，无需再等。要点：三生成模式作 provider capability 静态声明、沿用 video_tasks 表（已核实追加式，多 Take 无需修复）+ Shot 仅增 finalVideoTaskId、四硬规则（画幅继承/定稿落盘校验/成本闸门/视频默认不进 ZIP）+ 禁止静默降级 + motionPrompt 独立。**WP-Animatic 已合入**（`3dc2777`，Codex dc98b1c review PASS，worktree/分支已清）：AnimaticPlayer + buildAnimaticPlaylist（图链 finalizedImageUrl??generatedImageUrl??imageUrl、3s 兜底、缺 id 跳过、legacy videoUrl 拒传）。CC 复核 = 亲跑三件套 + 临时 demo 真机全交互验证（证据文件有 CC 增补节，含两则工具伪影澄清）。**接线已完成（c56ae25）**：交付步（第4步）"动态预览(Animatic)"入口 + onShotChange 联动 selectedShotIndex；items/回调 useMemo/useCallback 稳定化。**接线时实测修复一缺陷**：74 镜真机播放计时慢 25%——宿主 App 高频重渲染（90f64fd 引入轮询/FPS 类刷新源）连带重建播放器计时 effect、锚点重算累积丢时；修法=计时 effect 依赖收窄为原始值 + elapsedSec ref 锚点（AnimaticPlayer.tsx 内注释详述），修复后 97% 精度。**教训：全屏播放器类组件的计时 effect 依赖必须全为原始值，宿主重渲染频率不可假设。**
  **Video Lab M1 已合入并接线完成（真机全链路 PASS）**：合并 `6b50a21`（Codex 6629e7a review PASS，46/46+9/9 亲验）+ CC 接线 `5fae841`（generation_snapshot_json 迁移、createAgnesVideoTask 内核提取双路复用[直调路径回归逐字一致]、register、App 交付步 Video Lab 覆层）+ 证据增补 `e4c6fc5`。**真机（真实 Agnes，task af994104）**：providers→capability 驱动 UI→409 画幅三选层(1:1→3:2 crop)→真实生成→落盘 426KB→快照完整审计链全过；视频实体 ffprobe H.264+AAC/24fps/3.375s。worktree/分支已清。
  **M2 任务书已出（v1.1 可分发）**：`tasks/codex-video-lab-m2.md`，基线 `f801086`，分支 feat/video-lab-m2。范围=按 shot 查询/Take 并排/finalVideoTaskId 定稿(硬规则 B 五 code 矩阵)/批量+成本闸门(硬规则 C 三数字分开+服务端 confirmed 闸门)。**Animatic 混播定稿视频已被用户裁决砍除**（final-videos 端点+playlist 升级不做，AnimaticPlayer videoUrl 分支休眠；M1 备忘② durationSec 回填顺延 M3 消化）。deps 扩四项回调(mutateDb/listVideoTasksByShot/getVideoTask/isLocalVideoReadable)由 CC 实现。M3 留=视频交付/ZIP/重试 UI/Take 清理。
  **Video Lab M2 已合入并接线完成（真机 PASS，2026-07-16）**：合并 `f010dd5`（Codex 17/17+54/54）+ CC 接线（四 deps：mutateDb 复用/listVideoTasksByShot prepared 查询/getVideoTask=videoTaskRow/isLocalVideoReadable 复用 export-deck naming.ts getLocalPath+isReadableFile）。真机（真实 Agnes）：两镜成本闸门（三数字分开）→409 画幅 crop→批量串行；**Agnes 实测限流 1 req/min**，第二镜真机复现"部分失败保留审计不阻断"后单独重批成功；Take 倒序列表（含失败 Take 红标无定稿按钮）→双 Take 并排原生 video→两镜各自定稿（★徽标/取消/换选/批量列表联动排除）→落库核验双指针+独立 seed 快照一致。**备忘①再证**：定稿实物 ffprobe=1088x832@24fps/81帧/3.375s，provider normalized_size=1152x768 不符。**真机新发现（另立项）**：App 对 generated-scripts/comfyui-tasks 失控轮询（分钟级 1.4 万请求→ERR_INSUFFICIENT_RESOURCES 饿死页面 fetch/媒体），与 M2 无关；M3 备忘=批内节流应对限流。证据 `evidence/video-lab-m2-acceptance.md` §6。
  **前端失控轮询 bug 已修（28106f5，push origin）**：M2 真机验收时发现的既有缺陷（非 M2 引入）。根因=App.tsx 任务完成 effect（约 line 1095）从不回写 `prevComfyTasksRef` 基线，导致任何历史 succeeded comfy 任务每个 2s 轮询周期都被判成"刚完成"→ refreshGeneratedScripts → setGeneratedScript(新引用) → 轮询 effect 重启 → 立即再轮询，自我强化循环几分钟打出 1.4 万+ 请求 → 浏览器 ERR_INSUFFICIENT_RESOURCES 饿死所有 fetch/媒体加载（Video Lab 面板卡"正在加载"、并排定稿视频卡 readyState 0 皆源于此；服务端对同 URL HEAD 200 正常）。修法（6 行）：每轮无条件回写基线 + 首轮 `prevTasks.length===0` 只建基线不判 reload。真机复验：generated-scripts/comfyui-tasks 请求速率回落 ~0.5/s，并排视频加载到 readyState 4（1088x832/3.38s）。**教训固化：高频轮询 effect 若在其副作用里替换 effect 依赖项（此处 generatedScript），且判定逻辑依赖一个从不更新的 ref 基线，就会构成"副作用重启自身"的失控循环——轮询类 effect 的基线 ref 必须每轮回写，首轮只建基线。**
  **M3 任务书已出（可分发）**：`tasks/codex-video-lab-m3.md`，基线 `c4a22e8`，分支 feat/video-lab-m3。范围=①manifest/README 引用定稿视频（含 ffprobe 实测 actual 块，消化备忘①②，禁写 normalized_*）②可选打包进 ZIP 默认关（硬规则 D，delivery-check 回实际字节数供复选框）③导出二次落盘校验（硬规则 B，missing 不阻断只标注）④retry-download 端点+UI（Agnes URL 过期诚实失败）⑤Take 删除（TAKE_IS_FINAL/TAKE_IN_PROGRESS 守卫）⑥限流应对（capability minSubmitIntervalMs=61s + 成本层提示 + 失败镜头一键重试重走闸门；**CC 拍板不做服务端 sleep/排队**，可复议）。deps 新增 videoDelivery{getVideoTask,probeVideo} + redownloadVideo/deleteVideoTaskRow，由 CC 接线。允许文件=export-deck/**、video-lab/**、VideoLabPanel、DeliveryPanel、证据文档。
  **Video Lab M3 已合入并接线完成（真机 PASS，2026-07-16）**：合并 `35cd71f`（Codex 60/60，逐行 review PASS：normalized_* 隔离、manifest 剥离绝对路径、五失败码复用 M2 语义）+ CC 接线（export-deck `videoDelivery{getVideoTask,probeVideo}`；video-lab `redownloadVideo` 复用下载内核 / `deleteVideoTaskRow` 行+文件）。真机：delivery-check 字节和精确、不打包/打包双导出（manifest requested/actual 分离，actual=ffprobe 1088x832/3.375s，**备忘①②消化完毕**）、ZIP 含 videos/、retry 守卫+真实重下、DELETE 双守卫+合成行删除零残留、M1/M2 零回归。**环境坑固化**：preview 工具拉起的子进程 PATH 无 WinGet ffmpeg → `.env` 配 `FFMPEG_PATH`（正斜杠），代码支持 `FFPROBE_PATH` 覆盖；另录得一次性导出挂起异常（重启后 1.85s 复跑通过，对照组正常，非代码缺陷）。证据 `evidence/video-lab-m3-acceptance.md` §5。**Video Lab M1–M3 全里程碑收官**；剩余远期=服务端排队节流（若用户改判）、Agnes 静态图片 provider（独立任务书）。
  **Agnes 图片 Provider 已合回本分支（e2ee9e1）+ 前端适配任务书已出**：main 上已验收的图片 Provider/路由（PR #1/#3/#4/#5，autoRoute=true：主帧/含人物→comfyui_local、空镜→agnes 同步）合并进 feature/camera-derive；唯一冲突 server.ts import 区（并集）。后端本分支端到端 PASS（直调 /api/generate-image → agnes 同步、PNG 落盘 1.2MB、shot#72 审计字段写入；60/60+10/10、审计迁移 shots_updated=195）。**发现真实语义错位**：新创作向导 handleGenerateShotImage 的「ComfyUI 未连接即 throw」守卫（早于路由，App.tsx ~L2381）把 Agnes 路径挡死（真机复现零请求）——适配任务书 `tasks/codex-agnes-image-ui-adapt.md`（基线 e2ee9e1，分支 feat/agnes-image-ui-adapt）：守卫下移为「响应 taskId 后才查 runtime、未连接改警示不阻断」，forceProvider 两处强守卫保留，批量入口明确不动（后续包）。
  **适配包已合入（cd8ae61，真机 PASS）**：Codex 交付 review PASS（diff 仅 App.tsx+证据）→ 合并 → 真机：ComfyUI 离线+空镜 #74 新 UI 全链路真实 Agnes 落图+审计字段；含人物 #16 → 服务端 503 如实透出（**Codex 报的契约差异确认属实：真实后端离线不入队而 503，"入队+警示"分支现仅 stub 可达**）；autoRoute=false 回退开关真机验证（**配置启动时读取，切换必须重启**）后还原。遗留三项不阻断：服务端离线入队语义（独立 server 包待评估）、批量入口适配（后续包）、路由配置热加载。
  **批量入口适配任务书已出（可分发）**：`tasks/codex-agnes-batch-ui.md`，基线 `4b0fc26`，分支 feat/agnes-batch-ui，**纯前端包**。语义拍板（CC 定可复议）：在线批量一字不改；离线+missing 模式解除置灰 → preflight 拆分（items.matchedCharacters 空=Agnes 侯选）→ 确认层三数字（可生成 N/跳过 M/预计 N×45s）→ 前端串行逐镜走既有 /api/generate-image（body 与单镜路径复用禁漂移；服务端限流器/409 锁天然生效，前端禁自建限流）→ applyAgnesShotImage 落卡+进度+可停止+失败不中断；离线 failed/all 维持置灰。零 server 改动；长期方案（服务端路由拆分批量）归 server 包评估时整体替换。另：AGNES_API_KEY 轮换待办已由用户拍板关闭（不用改）。语义拍板①②（在线不分流/failed·all 离线不放开）用户已确认。
  **批量适配包已合入（dd71306，真机 PASS）**：review PASS（body 构造提取共用零漂移、runId+stop ref、脚本引用穿线）→ 真机：契约闸门 alert 命中、确认层三数字（9/39/405s）、#27 真实 Agnes 落图、**#37 真实内容审核拒绝（血迹词汇→502）失败不中断+审计持久化**、停止=当前镜完成后停+汇总正确。验证脚手架：契约三字段快照→临时锁定→停服手术还原（详见证据 CC 增补节）。**真机新知：Agnes 图片有内容审核，惊悚题材描述失败率不可忽视**。在线 ComfyUI 回归留待 ComfyUI 可用时抽查（低风险）。
  **三笔 Codex 直落主分支提交已 CC 补审通过并推送（2d03f04/811d9a8/7d09c0f）**：显式 Provider 选择器（auto/agnes/comfyui_local + Pollinations/Kling 统一下拉，forceProvider=agnes 服务端白名单已核）+ 单镜/批量真实进度 UI（不伪造百分比）+ Agnes 内容审核中文文案（502 响应带 provider 字段已核）+ 结构化参数初始化崩溃修复（localShot 快照统一）。真机抽查：五选项/徽标/按钮联动/强制本地离线禁用 PASS；lint+60/60。冗余分支 codex/agnes-explicit-progress（内容 ≡ 2d03f04+811d9a8 子集）已连 worktree 清理。**注意：Codex 本轮直接在主工作区分支提交，违反铁律 1（用户在场监督下发生）——下不为例，后续包仍强制 worktree。**
  **离线入队 server 包已合入真机全 PASS（4a612ed→b1a92f6，2026-07-17）**：CC 逐行审计=server.ts 改动严格限于两处授权热区（worker 走 preflight 显式分支、退避在认领前、等待日志去重正确）。真机三连：①离线 UI 强制本地入队（pending/waiting_for_comfyui，日志每任务一条）②路由配置**不重启热切换**生效（旧"须重启"结论作废）③**上线自动恢复闭环**——runtime/start 拉起真实 ComfyUI，2 分 12 秒后 [Worker] resumed → pulid 真实生成 succeeded 落盘（此前唯一 UNVERIFIED 项清零）。lint+70/70+11/11。证据 `evidence/comfy-offline-queue-acceptance.md` CC 增补节。
  **风格统一路线已定（用户拍板 2026-07-17）+ StyleBundle P0 任务书已出**：定性=当前混合路由是"生产效率路由"非交付级风格统一；分层锚点思想（风格/角色/场景/构图四通道独立，Midjourney sref/cref 先例）+ LTX"一致性=系统资产（命名/版本化/自动继承）"。路线：P0 双管线配方统一 → P1 双端风格锚图（**Agnes referenceImages 仅是入口存在、能力未验证，须先 A/B 确认不复制主体构图**；ComfyUI 侧需 IPAdapter 节点映射调研；锚图独立于人物母版；PuLID 保持独立通道）→ P2 定稿门查"同契约版本+同锚图版本+可接受配方+实物校验+人工确认"而非简单同 provider（CLIP/色彩相似度仅作离群警告）。交付策略：审阅稿混跑、交付稿在锚图验证前不承诺混合 provider 严格统一。**P0 任务书** `tasks/codex-style-bundle-p0.md`（基线 f996ef3，分支 feat/style-bundle-p0，**Codex 主导：deps 形状+server.ts 接线一并归 Codex（热区仅 registerImageGenRouting 调用点，用既有 import 组装不加新 import），验收须含隔离起服接线自验；CC 只 review+真机**）：StyleBundle 类型+组装、Agnes 注入与 ComfyUI 逐字同框架语同去重、**契约尺寸继承（实锤缺口：Agnes 现恒 1024×1024 无视契约，routes.ts L148）**、快照+gen_style_contract_version 零迁移追踪、resolveStyleContext deps 由 CC 接线、风格上下文失败不阻断生成。
  **StyleBundle P0 已合入真机 PASS（86b3003，2026-07-17）**：Codex 主导交付（含 server.ts 接线自验），CC review=框架语与 server.ts L7216/L7226 逐字一致、尺寸继承 `===undefined` 判定、版本写/清机制、rawMeta8k 字节感知截断保留摘要；单 hunk 零越界。真机（真实 Agnes 空镜）：响应带 styleContractVersion、shot 落 gen_style_contract_version、无 style_context_unavailable（resolver 在位）、尺寸走继承（草稿派生 1024；锁定契约 640×384 注入由接线自验以 version 7 证过）。lint+70/70+19/19。证据 `evidence/style-bundle-p0-acceptance.md` CC 增补节。
  **风格锚图 P1-A 任务书已出（可分发，Codex 主导）**：`tasks/codex-style-anchor-p1a.md`，基线=P0 合入后 HEAD。**只做注入前置的资产层**（项目级独立风格锚图存储/版本/随 StyleBundle 继承，绝不复用人物母版），**绝对红线=零「锚图→生成」注入**（referenceImages/IPAdapter/prompt 拼接任一即越界返工）。注入等 CC 并行 A/B 调研（Agnes referenceImages 是否复制主体构图 + ComfyUI IPAdapter 节点映射能力）结论后另出 P1-B。零迁移 styleAnchor 可选字段 + 新 style-anchor 模块 + imageGen 快照扩展 + 前端风格设定锚图入口（诚实标注"注入未启用"）。
  **Agnes referenceImages A/B 已做完，结论决定性（2026-07-17 CC 真机）**：`referenceImages` **不是风格参考而是主体/构图参考**——冷暗电影锚图 + 明亮野餐 prompt → 输出风格完全没迁移、反把锚图那只按鼠标的手+机位原样嫁接进野餐场景（证据 `evidence/agnes-referenceimages-ab-finding.md`，产物 shot-2-a60939f9[A 无锚]/shot-2-2f9e1a5b[B 有锚]/锚图 shot-9-2b94c5d2）。**P1-B 因此定向单端：风格锚图只走 ComfyUI IPAdapter，Agnes 端锚图注入取消、维持 P0 prompt overlay**。P1-A 资产层不受影响（ComfyUI 要消费、P2 定稿门要判版本）。**交付策略收紧**：Agnes 空镜风格统一仅靠 overlay 弱杠杆，交付级严格统一以 ComfyUI 配方为准。用户拍板 Agnes 免费、A/B 无计费顾虑。
  **ComfyUI 节点映射调研已做完（2026-07-17 CC，8001 在线）**：证据 `evidence/comfyui-ipadapter-capability-finding.md`。IPAdapter/PuLID/Redux 全装了，**但**干净风格分权重节点 `easy ipadapterStyleComposition` **仅 SDXL**；进 ComfyUI 的镜头（master+人物，空镜走 Agnes）是 **Flux2(klein)**，Flux2 风格参考只有 `StyleModelApply`(Redux)——纯度未验、Redux 惯抄构图=同 Agnes 风险类；现状人物=Flux2 PuLID / 非人物默认=SDXL 已多族并存。**无统一风格锚点覆盖全镜**。**P1-B 卡在用户拍板**：(a)全镜 SDXL（弃 Flux 画质/弱身份）(b)Flux2+Redux（前置=Redux A/B 未跑）(c)分层折中（锚点只上 master 镜/人物镜靠 PuLID+overlay+人工）。**交付级混合 provider 严格统一目前无现成技术路径**，用户原策略是唯一稳妥落法。P1-A 资产层不受影响可继续。
  **ComfyUI 离线入队 server 包任务书已出（可分发）**：`tasks/codex-comfy-offline-queue.md`，基线已随 M3 hardening 顺移到 `86b36d8`，分支 feat/comfy-offline-queue。**server.ts 首次热区特批（用户拍板）**：仅两处——①/api/generate-image 离线 503 预检改为记录不阻断（任务照建，stateDetail=waiting_for_comfyui）②worker 把「未连接」从 failed 改为回退 pending 等待 + 30s 探测退避（其余错误 failed 语义逐字不变）。附带：imageGen 路由配置 mtime 热加载（坏 JSON 沿用旧配置）+ 前端强制本地离线按钮解禁（入队+警示）。诚实边界：「上线自动恢复」无真 ComfyUI 不可验，UNVERIFIED 留 CC。
  **M3 hardening 包已经用户说明后走正常 review 合入（caaa740 cherry-pick 为 86b36d8，基于 35686b2）**：来源=用户委托 Codex 对 M3 测试做独立复核（任务 019f6d94），其复核把原「全收官」降级为 **CONDITIONAL PASS**，三处过度结论获采纳：①`FFMPEG_PATH` 目录无 ffprobe 兄弟文件，冷启动稳定性此前未证——现补 ffprobe.ts（FFPROBE_PATH 覆写/兄弟存在性解析/启动 `-version` 自检/严格 parse），不可用时优雅降级 actual=null 并警示；②10 分钟 PPTX 挂起改记 **UNRESOLVED FLAKE**——现补 11 阶段导出诊断回调（onExportPhase，failed 附 failedPhase+errorCode），再挂起可定位；③过期 Agnes URL 真机重下仍 **UNVERIFIED**。另补：同毫秒导出目录原子预留（EEXIST 重试）。评审记录见 `evidence/video-lab-m3-acceptance.md` §6。**2026-07-17 合入后真机重启 dev server，启动日志命中 `[ffprobe] ready (source=FFMPEG_PATH_SIBLING)`，真实 sibling 的 `-version` 自检通过，冷启动缺口已闭环**；历史 PPTX 挂起根因与过期 Agnes URL 仍按原口径保留未闭环。
  **工作区未提交改动 — 已全部落定，工作区现干净（2026-07-16 收尾）**：该批用户本人 WIP（故事创意编辑、分镜重生成、图片加载优化、ComfyUI 打开/工作流下载/PNG 导回/请求恢复）随各里程碑陆续提交；最后残留的 `src/index.css` 已由 CC 审查（分镜工作台三栏栅格响应式断点：桌面第三栏 minmax(260,280)、≤1200 两栏上下文换行、≤860 单列堆叠；三档真机复验符合预期、无死选择器无级联冲突）后，以用户身份提交 `a38dea2`（未挂 Claude co-author，如实归属）。**推进顺序（用户拍板，均已完成）**：①审查落定这批改动 ✓ → ②Animatic 已合入 → ③M1 基线已填 → ④M1/M2 均已启动并接线真机 PASS。另：**Agnes 静态图片 provider（生成分镜图）是独立需求，另开任务书，不混入视频任务书**。
- **风格锚点 IPAdapter**（方案 §六.4，P3 时判为需 ComfyUI 工作流预设扩展而后置）：定稿首图作风格参考注入后续分镜。属"真开放注入"范畴，先确认 manifest/工作流映射能力再评估。
- **场景参考增强**：现为轻量版（纯文本 overlay 注入，图不参与 conditioning）；若需要图像 conditioning 再扩。

## 已知遗留（不阻断）

- **游离分支/worktree**：`.pnpm-store/worktrees/style-contract-integration`（分支 `feat/style-contract-integration@f171884`）**未合入主线**，来源存疑、非本轮 CC 创建——清理前须先确认是否含未合并工作，**勿盲删**。另有 Agnes 相关分支（`codex/agnes-*`、`feat/video-retry-download`）与 `feat/ui-v2-integration`（批次1 旧集成分支，已合入可删）、`fix/fresh-db-import-sha256` 等，均非本轮范围。
- **export-deck 跟进包已合入**（`d362168`，Antigravity bdaaa3d review PASS）：naming.ts 单一事实源（sanitizeFilename/getLocalPath/isReadableFile/sceneExportFile 收编，CC 预比对逐行等价后授权直并）+ emoji 截断 fixture 补真（18 码点、边界落 ZWJ 序列首码点）+ manifest↔zip 双向一致性回归用例（含角色目录抽查）。CC 复核全套：自跑 lint/build/6为6、真机 zip Expand-Archive 零乱码（证据 `evidence/export-deck-followup-acceptance.md` §4）。合并后全模块 37/37（=旧 36 + 新增 1）。worktree/分支已清。**遗留记录（P3 级，未立项）**：manifest.shots[].imageFile 的 `finals/shot-XX` 模式（generator.ts）与 routes.ts finals 拷贝疑似同类双推导，未来评估是否收进 naming.ts。
